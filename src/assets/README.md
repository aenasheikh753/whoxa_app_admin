# Static Assets

Static files used in the application:

- `images/` - Image assets (PNG, JPG, SVG, etc.)
- `icons/` - Icon files (SVG, icon fonts)
- `fonts/` - Custom font files

Optimize all assets for web use before adding them here.
