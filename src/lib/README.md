# Library Configurations

Third-party library configurations and utilities:

- `queryClient.ts` - React Query client configuration
- `axios.ts` - Axios HTTP client setup
- `validations.ts` - Centralized validation schemas (e.g., using Yup or Zod)

This directory contains configurations and custom setups for third-party libraries used in the application.
