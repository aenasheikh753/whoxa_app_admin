# Pages

Page components organized by feature:

- `auth/` - Authentication related pages (login, register, forgot password)
- `dashboard/` - Main application dashboard and its sub-pages
- `settings/` - User and application settings pages

Each page should be a route component that composes multiple components together.
