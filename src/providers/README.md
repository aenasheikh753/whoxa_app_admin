# Context Providers

React context providers for the application:

- `ThemeProvider.tsx` - Manages theme context
- `QueryProvider.tsx` - React Query provider configuration
- `AuthProvider.tsx` - Authentication context provider

These providers wrap the application to provide global state and functionality.
