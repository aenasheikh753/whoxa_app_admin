# Services

API and external service integrations:

- `api/` - API client setup and endpoint definitions
- `auth/` - Authentication and authorization services
- `storage/` - Local/session storage utilities

This directory contains all the service layer code that interacts with external APIs and services.
